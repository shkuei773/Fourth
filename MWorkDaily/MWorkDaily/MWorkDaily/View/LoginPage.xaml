<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MWorkDaily.View.LoginPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:dx="clr-namespace:DevExpress.Maui.Core;assembly=DevExpress.Maui.Core"
    xmlns:dxe="clr-namespace:DevExpress.Maui.Editors;assembly=DevExpress.Maui.Editors"
    xmlns:dxg="clr-namespace:DevExpress.Maui.DataGrid;assembly=DevExpress.Maui.DataGrid"
    xmlns:local="clr-namespace:MWorkDaily.ViewModel">

    <ContentPage.BindingContext>
        <local:LoginPageViewModel />
    </ContentPage.BindingContext>

    <dx:DXStackLayout Padding="10" VerticalOptions="Center">
        <dxe:TextEdit
            Margin="10"
            LabelText="URL"
            PlaceholderText="SuperBase URL 입력"
            Text="{Binding SuperURL, Mode=TwoWay}" />
        <dxe:TextEdit
            Margin="10"
            LabelText="API"
            PlaceholderText="SuperBase API 입력"
            Text="{Binding SuperAPI, Mode=TwoWay}" />
        <dx:DXButton
            Margin="10"
            BackgroundColor="AliceBlue"
            BorderColor="DarkGray"
            BorderThickness="1"
            Command="{Binding saveCommand}"
            Content="접 속"
            CornerRadius="1"
            FontSize="20"
            IsEnabled="{Binding IsEnable}"
            TextColor="DarkGray" />

    </dx:DXStackLayout>

</ContentPage>